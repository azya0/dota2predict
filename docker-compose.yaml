services:
  db:
    container_name: db
    image: postgres
    restart: always
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:${POSTGRES_PORT}"
    healthcheck:
      test: ["CMD-SHELL", "sh -c 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}'"]
      interval: 1s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer
    restart: always
    ports:
      - "${ADMINER_PORT}:${ADMINER_PORT}"
    command:
      - 'php'
      - '-S'
      - '[::]:${ADMINER_PORT}'
      - '-t'
      - '/var/www/html'
    depends_on:
      - db

  migrate_tables:
    container_name: migrate_tables
    restart: no
    env_file:
      - .env
      - .env.database
    build:
      context: backend/database
      dockerfile: ../../dockerfiles/migration/Dockerfile
    depends_on:
      db:
        condition: service_healthy

  collect_heroes:
    container_name: collect_heroes
    restart: no
    env_file:
      - .env
      - .env.database
    build:
      context: backend
      dockerfile: ../dockerfiles/collect/Dockerfile
    depends_on:
      db:
        condition: service_healthy
    command: python collect_heroes.py
  
  collect_pro_matches:
    container_name: collect_pro_matches
    restart: no
    env_file:
      - .env
      - .env.database
    build:
      context: backend
      dockerfile: ../dockerfiles/collect/Dockerfile
    depends_on:
      - collect_heroes
    command: python collect.py
